extends Control

var Messaging
var PlayerProfile
#const BULLET = preload("res://Bullet.tscn") #this line goes in your fields section

# Called when the node enters the scene tree for the first time.
#func _ready()
	#var bullet_instance = BULLET.instance() # this creates and instance
	#add_child(bullet_instance)
	
# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
